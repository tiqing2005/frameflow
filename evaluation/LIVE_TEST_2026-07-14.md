# DeepSeek Live Integration Test Evidence

- Date: 2026-07-14 (Asia/Shanghai)
- Test: `tests/test_llm_live.py::test_deepseek_returns_segments_without_degradation`
- Provider configuration: OpenAI-compatible DeepSeek endpoint
- Model: `deepseek-v4-pro`
- Result: **PASSED**
- Duration: 9.67 seconds

Verified assertions:

- The provider returned non-empty semantic segments.
- The result used the real provider path rather than the rules fallback.
- `degraded` was `false`.
- Concatenated segment text preserved the complete source transcript.
- The configured API key was not present in any returned error message.

Command shape used for verification:

```text
FRAMEFLOW_RUN_LIVE=1 python -m pytest -m live tests/test_llm_live.py -s -vv
```

No API key or secret value is stored in this evidence file.
