This is deliberately plain text with a .jpg extension.
FrameFlow should reject it because the file signature does not match a real JPEG image.
Expected result: a structured 4xx upload validation error and no orphan database record.
