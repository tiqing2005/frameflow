# FrameFlow AI 已知问题与取舍

## 1. 文档目的

本项目的完成时间受限，以核心业务闭环和故障可恢复性为优先级。下列内容是已知边界，不会在演示中隐藏或包装成已完成能力。

## 2. 功能边界

### KI-01：默认环境未配置真实 ASR

- **影响**：音频/视频可上传并建立异步任务，但无 Provider 时会以 `ASR_NOT_CONFIGURED` 失败，不会生成字幕。
- **当前处理**：文本粘贴是稳定、真实的主演示入口；失败界面提供原因与重试。
- **后续方向**：实现 `Transcriber` Provider，由 ffmpeg 抽取 16 kHz 单声道音频，对接云 ASR 或独立 faster-whisper 服务；增加时长、费用、超时和真实音频测试。

### KI-02：默认匹配不是神经语义 Embedding

- **影响**：抽象隐喻、跨语言和同义改写的匹配能力有限。
- **当前处理**：字符 n-gram TF-IDF 向量、关键词和标签/主题重合混排，每个分量都可解释且可离线运行。
- **后续方向**：增加多语言 Embedding Provider，保留关键词通道，用标注样本调整权重而非主观改分。

### KI-03：未自动识别素材画面标签

- **影响**：新素材的名称、标签和关键词需人工填写；排序依赖这些元数据。
- **当前处理**：表单对必填信息给出明确提示，预置素材使用人工复核的中文元数据。
- **后续方向**：对图片抽帧/视频关键帧调用视觉模型，生成建议标签后由人工确认，而非直接覆盖。

### KI-04：静态组合预览不是精确视频时间轴

- **影响**：不提供帧级剪辑、字幕轴对齐、转场、音轨或导出预览视频。
- **当前处理**：工作台聚焦“片段→候选→最终选择”，避免与原题不要求的完整剪辑器混淆。
- **后续方向**：在 ASR 可提供稳定词/句时间戳后，再建立时间轴领域模型与异步渲染队列。

## 3. 工程边界

### KI-05：SQLite 和单 Worker 不适合水平扩容

- **影响**：当前吞吐量受单 Worker 和单持久卷限制，不应同时启动多个跨机写实例。
- **当前处理**：WAL、`busy_timeout`、持久化队列、原子领取与租约恢复，完成 Demo 层面可靠背压。
- **后续方向**：PostgreSQL + 业务 outbox + Redis/RabbitMQ 队列，再开启多 Worker。

### KI-06：上传文件使用本地持久卷

- **影响**：只能部署为可挂载持久磁盘的单实例；磁盘清理会造成素材与数据库不一致。
- **当前处理**：数据库和文件位于同一 `/data` 持久卷，不部署到临时 Serverless 文件系统。
- **后续方向**：S3/R2/OSS 对象存储、预签名上传、文件引用计数与定期清理。

### KI-07：公网 Demo 不等于完整多租户权限系统

- **影响**：若未在部署层配置访问口令、配额和限流，公网上传端点可被滥用。
- **当前处理**：演示环境应由平台/HTTP 基础认证限制访问，且使用专用演示数据。
- **后续方向**：用户会话、项目归属、RBAC、配额、速率限制和审计管理员。

### KI-08：媒体安全检查不等于恶意文件扫描

- **影响**：大型或恶意构造媒体仍可能消耗解码器资源。
- **当前处理**：文件大小、扩展名/MIME 允许列表和安全随机文件名。
- **后续方向**：隔离解码容器、CPU/内存/时间限制、媒体探测和恶意文件扫描。

### KI-09：任务进度采用轮询

- **影响**：任务数量很大时会产生额外请求，事件到达不是毫秒级。
- **当前处理**：Demo 任务数小、更新周期受控，轮询在公网代理下更稳定。
- **后续方向**：服务端事件或 WebSocket，保留轮询作断线回退。

## 4. 测试与部署状态

不应把“测试文件存在”等同于“测试已通过”。每次正式提交前需在 `TEST_PLAN.md` 的执行记录中填入实际 commit、环境、日期与结果。

公网地址、测试账号和视频链接只能在真实可访问后写入提交信息，不应在文档中预先伪造。

## 5. 发布前必须更新本文的情况

- 真实 ASR/LLM/Embedding Provider 已配置并有可重复测试证据。
- 新增身份认证、限流或对象存储。
- 新增预览视频或精确时间轴。
- 实际部署不使用持久卷，或使用了多 API/Worker 实例。
- 任何已知问题的影响或缓解方式与实际代码不再一致。
